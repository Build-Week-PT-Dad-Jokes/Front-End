

const initialState = {
  jokes:[],
  isSearching: false,
  searchResponse: null
}

export const jokesReducer = (state = initialState, {type, payload}) => {
  switch (type) {
    default:
      return state;
  }
}